slug: temporal-go-hands-on
title: Temporal Go SDK – Hands-On Lab
teaser: Build a durable order-fulfillment system with the Temporal Go SDK — activities,
  child workflows, parallel fan-out, and local activities.
description: |-
  Four progressive coding challenges using the Temporal Go SDK. Each challenge drops you into a
  partially-implemented order fulfillment workflow and asks you to fill in the missing pieces.

  **What you'll build:**

  - **Exercise 1 – Converting a Workflow:** Replace a fragile retry loop with Temporal Activities and a durable Workflow.
  - **Exercise 2 – Child Workflows:** Extract inventory reservation into a dedicated child workflow with its own history and retry boundary.
  - **Exercise 3 – Parallel Activities:** Fan out warehouse checks concurrently with `workflow.Go()` goroutines and a `workflow.Channel`.
  - **Exercise 4 – Local Activities:** Move fast in-process steps to local activities to reduce Temporal Cloud Actions.

  Each challenge includes a **Web UI** tab so you can watch your workflows execute in real time, and a **Solve** button if you get stuck.

  **Prerequisites:** Basic Go familiarity. No prior Temporal experience required — the exercises are self-guided.
icon: https://storage.googleapis.com/instruqt-frontend/img/tracks/default.png
tags:
- temporal
- workflows
- go
owner: temporal
developers:
- marcus.merrell@temporal.io
idle_timeout: 1800
timelimit: 7200
lab_config:
  sidebar_enabled: true
  feedback_recap_enabled: true
  feedback_tab_enabled: true
  loadingMessages: true
  hideStopButton: false
  default_layout: AssignmentLeft
  default_layout_sidebar_size: 35
enhanced_loading: false
